-- chunkname: @scripts/settings/mechanism/mechanism_settings_syntax_check.lua

local function syntax_check(mechanism_settings)
	for name, settings in pairs(mechanism_settings) do
		-- Nothing
	end
end

return syntax_check
