local point_of_interest_settings = {
	default_view_distance = 10,
	broadphase_cell_size = 30,
	seen_recently_threshold = 15,
	max_view_distance = 50,
	event_trigger_interval = 1
}

return settings("PointOfInterestSettings", point_of_interest_settings)
